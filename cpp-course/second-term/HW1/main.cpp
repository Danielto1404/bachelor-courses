#include <iostream>
#include "const.h"
#include <fstream>

int main(int argc, char** argv)
{
    printf("Number of arguments : %d\n", argc);
    printf("Filepath : %s\n", argv[0]);
    printf("First argument : %s\n", argv[1]);
    printf("Const e = %f\n", e);
    return 0;
}

// command delete || command X
// command D - copy last
// command B - declaration
// Shift + fn + f6
